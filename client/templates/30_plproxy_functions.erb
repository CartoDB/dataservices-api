CREATE OR REPLACE FUNCTION <%= GEOCODER_CLIENT_SCHEMA %>._<%= name %> (username text, <%= params_with_type %>)
RETURNS <%= return_type %> AS $$
  CONNECT <%= GEOCODER_CLIENT_SCHEMA %>._server_conn_str();
  SELECT cdb_geocoder_server.<%= name %> (username, <%= params %>);
$$ LANGUAGE plproxy;

